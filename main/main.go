package main

import (
	"fmt"
)

func main() {
	// scanner := bufio.NewScanner(os.Stdin)
	// _ = scanner.Scan()
	// text := scanner.Text()
	a := "!"
	b := "1"
	c := "*"
	if a > b {
		if a > c {
			fmt.Println(a)
		} else {

		}
	}
}
